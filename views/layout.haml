!!!
%html
  %head
    %title= @config.ssid
  %body
    %h1= @config.ssid
    %h2= @service.status

    %form(action="/toggle" method="post")
      %button= @service.active? ? :disable : :enable

    %hr

    %h3 hostapd config
    %table
      - @config.config.each do |key, value|
        %tr
          %td= key
          %td= value

    %hr

    %h3 service
    %table
      - @service.properties.each do |key, value|
        %tr
          %td= key
          %td= value

